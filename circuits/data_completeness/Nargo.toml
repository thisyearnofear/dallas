[package]
name = "data_completeness"
type = "bin"
authors = ["Dallas Buyers Club"]
compiler_version = ">=0.36.0"
description = "Proves that required optimization log fields are present without revealing content"

[dependencies]
